import express from "express";

import { allstudents, newstudents, updatestudent, deletestudent } from "../controllers/student";

const router = express.Router();

router.get('/all', );
router.post('/create', );
router.put('/update', );
router.delete('/del', );

export default router;